name: ShawcliffeSeller
options:
  bundleIdPrefix: ca.shawcliffe
  deploymentTarget:
    iOS: "16.0"
  createIntermediateGroups: true

packages:
  Supabase:
    url: https://github.com/supabase/supabase-swift
    from: 2.0.0

targets:
  ShawcliffeSeller:
    type: application
    platform: iOS
    sources:
      - path: ShawcliffeSeller
    dependencies:
      - package: Supabase
        product: Supabase
    info:
      path: ShawcliffeSeller/Info.plist
      properties:
        CFBundleDisplayName: Shawcliffe Seller
        UILaunchScreen: {}
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: ca.shawcliffe.seller
        PRODUCT_NAME: ShawcliffeSeller
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "5.0"
        TARGETED_DEVICE_FAMILY: "1,2"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        DEVELOPMENT_TEAM: M5SD2YQ3QM
        CODE_SIGN_STYLE: Automatic
