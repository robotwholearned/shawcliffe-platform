// Single home for the input styling that was copy-pasted across every form.
// Focus ring uses the injected --brand-primary CSS var (via Tailwind's brand.primary).
export const inputBase =
  'w-full rounded-xl border border-gray-200 bg-white px-4 py-3 text-sm text-gray-900 placeholder:text-gray-400 transition-colors focus:border-brand-primary focus:outline-none focus:ring-2 focus:ring-brand-primary'

export const labelBase = 'block text-sm font-medium text-gray-700 mb-1'
