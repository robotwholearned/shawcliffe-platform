services:
  - type: web
    name: shawcliffe-platform
    runtime: node
    rootDir: platform/web
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_BASE_DOMAIN
        value: shawcliffedigital.com
        # Storefronts: {slug}.shawcliffedigital.com
        # Admin/seller: accessed via any subdomain not matching a client slug
      - key: NEXT_PUBLIC_SUPABASE_URL
        sync: false
      - key: NEXT_PUBLIC_SUPABASE_ANON_KEY
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
